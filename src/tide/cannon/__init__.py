"""tide.cannon — durable truth home (CANON.md living-IS doc).

Spelling: cannon = two N (the weapon), distinct from the camera brand. Planned
modules (later units):
  store.py   — cannon/ init (CANON.md + config), read / scan (U2)
  rev.py     — cannon-rev = short sha256 of CANON.md ONLY (the truth) (U2)
  merge.py   — merge_delta(): append delta under '## Cannon journal' (U2)
  board.py   — cannon status: scan per-arc homes, group by 5-state (U8)

cannon-merge is the single serialization point and is ORCHESTRATOR-ONLY.
"""
